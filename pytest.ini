[pytest]
testpaths = tests
python_files = test_*.py
python_functions = test_*
pythonpath = .
addopts = -v --tb=short -m "not slow"
markers =
    slow: marks tests that require network or GPU
    unit: fast isolated unit tests
